#include "main.h"

/**
 * _abs - Computes absolute value of the integer 'n'.
 *
 * @n: Integer value whose absolute value will be computed
 *
 * Return: Absolute value of 'n'.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
