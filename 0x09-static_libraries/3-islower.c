#include "main.h"

/**
 * _islower - Evaluates if character is a lowercase letter
 * @c: Character to be evaluated
 *
 * Return: 1 if character is lowercase letter, 0 if otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
